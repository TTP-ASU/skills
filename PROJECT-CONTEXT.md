# Project context (workspace)

**Active project: SSOT-SUR.** Notion page IDs, meeting database URL, local PRD/TRD paths, and Notion MCP setup are maintained in:

**→ [`project/ssot-sur/PROJECT-CONTEXT.md`](project/ssot-sur/PROJECT-CONTEXT.md)**

Read that file for discovery workflows (`capture meeting`, `push PRD`, etc.). This root file exists only so agents that open the repo root still land on the correct project context.

---

*For a blank template when starting a different project, copy from history or ask to scaffold a new `project/<name>/PROJECT-CONTEXT.md`.*
