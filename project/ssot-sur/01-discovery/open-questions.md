# Open questions – SSOT-SUR

Track open questions here. When a question is answered, update **Status** to Resolved, fill **Answer** and **Date**, then update the relevant section in the Brief or PRD.

Grouped by theme. **Architecture questions** (from design/engineering sessions) are flagged with `[ARCH]` and must be resolved before the relevant functional requirement can be fully specified.

---

## A. Peril Mapping & Equipment Type

| # | Question | Source | Owner | Status | Answer | Date |
|---|----------|--------|-------|--------|--------|------|
| A1 | Who owns peril-to-equipment-type translation: DAX or ServiceBench? **Context:** When Horizon sends a repair request, it includes a "crack/peril class" (e.g., cracked screen, battery failure). DAX consumes **part types** (e.g., LCM, BAT), not raw perils. **TELUS "bypass inventory" when no parts exist remains B1/B2 — not A1.** | Meeting: DAX-SB Integration (Mar 6); **Review Open Questions — Tech Sync 2026-03-20** | Ankit / DAX Arch | **Resolved** | **Tech Sync 2026-03-20:** **ServiceBench owns peril → part-type mapping** (e.g., crack screen → LCM). Flow: Cloud API sends peril to Prism; Prism sends product / equipment type to DAX; SB maps peril to part types (LCM, BAT, multi-peril) and passes **part types** into DAX APIs (DAX does not accept perils in the BOM call today — confirmed with Raghu/Matt). **DAX owns** part-type filtering on BOM and job-type exclusion on part mapping; **SB owns** how job-type exclusion affects repair eligibility. **Client is not part of peril mapping** (client appears in substitution matrix — separate). Michelle: prior AI phrasing "convoluted"; formal API contract line-by-line in **Wed** session same week; Michelle documenting MA-style request/response. | 2026-03-20 |
| A2 | **[ARCH]** Should the "bypass inventory check" logic be driven by peril + client configuration in DAX (recommended), or should ServiceBench own the client-level config to proceed without a BOM? **Context:** For some perils (e.g., certain TELUS scenarios), no inventory check should happen because no parts exist in DAX. **Note:** Peril→part-type mapping is **not** client-scoped (Tech Sync 2026-03-20); **substitution matrix** is where client can appear — separate from peril. | Meeting: DAX-SB Integration (Mar 6); **Tech Sync 2026-03-20** | Ankit / DAX Arch | Open | **Tech Sync 2026-03-20:** Clarified **peril path has no client dimension** for the mapping in question. **TELUS / non-LCM "bypass"** (when no parts in DAX) remains a **Plan A vs B** decision — see **B1/B2**. For **generic** bypass flag in DAX response vs SB config: still align with EA direction (SB adjudicates, DAX provides data); confirm when **B1/B2** close. | |
| A3 | OEM parts equipment type: what is the correct equipment type for OEM SKUs in F&O/DAX for pricing and charging logic? Needs verification before BOM can return OEM SKUs. **Context:** In ServiceBench today, OEM parts are a separate part category with specific equipment type mappings that drive pricing and client charging. When parts move to DAX, the equipment type classification must carry over correctly — otherwise the BOM will fail to return OEM options or price them incorrectly. MDM and SCM must confirm the correct F&O equipment type for each OEM part class. | Meeting: DAX-SB Integration (Mar 6) | MDM / SCM | Open | | |
| A4 | Can job type exclusions (currently at SKU level in ServiceBench) be modeled at equipment-type level in F&O/D365 instead, or must they remain SKU-level? | Meeting: JIT (Mar 6); EA Support (2) 2026-03-20; **Tech Sync 2026-03-20** | Robbie Carter / DAX | **Resolved** | **Tech Sync 2026-03-20 — closed:** Store job type exclusion as **SKU-level attribute in DAX**. MDM already has access; **Robbie** may need **security/role changes** to update SKU attributes on the fly (vs waiting on offshore MDM). **No new UI** — use **existing DAX form**; field mapping TBD as design progresses. Michelle to include **job-type exclusion + Robbie security** in relevant **PBI/feature scope**. Confirms **SKU-level** (not equipment-type-level) for this control. | 2026-03-20 |
| A5 | Job type exclusion **dimensions** (client / market / model): What keys are in the DAX/F&O config and in the availability API vs what ServiceBench must interpret? **Context:** **EA Support (2) 2026-03-20** aligned: DAX holds job-type exclusion / part master **data**; DAX exposes attributes via API; **ServiceBench makes eligibility determinations** (not duplicate part data across 5–6 systems). **Tech Sync 2026-03-20** closed **D3** (exclusions **in response**). **Job Type / Eligibility Logic 2026-03-23** added: response lists **excluded** job types; **no phone model** in current SB exclusion UI; **client vs market** — define in **Wed/Thu API contract** session (week of 2026-03-24). Optional: **audit trail** for exclusion changes — confirm NFR with DAX. | EA Support (2) 2026-03-20; Tech Sync 2026-03-20; Job Type / Eligibility 2026-03-23 | DAX / Michelle / Robbie | Open | **D3 resolved** — exclusions in response. **Job Type / Eligibility 2026-03-23 confirmed:** Exclusions defined at **part + client (purchase from / client group)** level — **no phone model** dimension. API response lists **excluded** job types per part per client (e.g., "no CSS JIT", "no remote tech"). Each substitute part from the substitution matrix has its own exclusion record in the response. DAX provides data only; SB retains all eligibility adjudication logic. **Audit trail:** transaction logging to track who updated exclusions and when. **Client vs market:** Market S111 example feeds 3 clients (NRG, Allied, Vivint) — shared markets require identical behavior; "client group" maps to SB "purchase from" value. Final client vs market dimension **deferred to Wednesday API contract workshop (week of 2026-03-24)**. | 2026-03-23 (partial) |

---

## B. TELUS / Mobile Clinic (Canada) Architecture

| # | Question | Source | Owner | Status | Answer | Date |
|---|----------|--------|-------|--------|--------|------|
| B1 | **[ARCH]** TELUS/Mobile Clinic only provides LCM inventory. For non-LCM perils (battery, charging port, camera), no BOM/availability check happens today, and jobs are created without part lines. Plan A: push TELUS to provide full inventory for all part types. Plan B: return a "bypass inventory check" flag per peril+client. Which path? **Context:** TELUS and Mobile Klinic have different parts sourcing from UBIF — they only have LCM parts in DAX. For battery or camera repairs, there are no parts in the system, so today SB creates the job with no parts. If this continues in the future state, the API needs to gracefully handle the "no BOM" case. If TELUS is pushed to full inventory, it requires a TELUS onboarding effort that is a separate workstream and may be out of scope for Phase 1. | Meeting: DAX-SB Integration (Mar 6) | Ankit / DAX / TELUS rel. | Open | Part Rel Mgmt 2026-03-19 confirmed: TELUS/Mobile Klinic have no part prioritization matrix due to different parts sourcing. No update on bypass path decision. | |
| B2 | **[ARCH]** Should the "bypass inventory" flag live in DAX's BOM response or in ServiceBench's client-level config? **Context:** Follows from B1. If a bypass flag is needed for TELUS non-LCM perils, the architecture must decide where the flag lives. DAX option: cleaner API contract, bypass returned in availability response. SB option: faster to implement, no new DAX data needed, SB config already exists for some client-level overrides. Relates to A2 — same decision pattern. | Meeting: DAX-SB Integration (Mar 6) | Ankit / DAX Arch | Open | | |
| B3 | What is the timing of TELUS CRM project migration, and does that change the preferred path for inventory integration? **Context:** TELUS is undergoing a CRM migration that may change how they interact with Asurion systems. If CRM migration overlaps with the SSOT timeline, the preferred integration pattern (RTI repoint vs. new API) may need to account for their CRM transition to avoid double-rework. Need TELUS relationship lead to confirm timeline. | Meeting: DAX-SB Integration (Mar 6) | Business / TELUS rel. | Open | EA Support (2) 2026-03-20 noted: TELUS integration expected to be complex ("head scratching"). Team considering whether to solution TELUS upfront in discovery or defer to a later phase. Sequencing decision pending. | |
| B4 | Mexico/Mobile Clinic: currently use RTI webservice (web service calls, not file-based). Will they switch to AWS/JSON as part of this project? Who takes over the web service when feeds move to DAX? **Context:** Most NAOP vendors use file-based inventory feeds (ETL). Mexico and Mobile Clinic are different — they use RTI webservice API calls. This means a simple file-to-DAX migration won't work for them. Two paths: (A) Mexico moves to FSG directly (new integration work); (B) RTI routes through FSG to DAX (wraps existing RTI, less Mexico-side change). Part Rel Mgmt 2026-03-19 action item: determine if Mexico vendors can move to FSG or if RTI needs to route through FSG to DAX. | Meeting: DAX-SB Integration (Mar 6) | SB Eng / DAX | **Partially resolved** | **DDD-SUR architecture (2026-03-26):** "Any future Tyk/FSG migration where possible will be handled by the Service Bench team. And Service Bench Team will route the data to DAX API for inventory visibility service ingestion." **SB team owns the RTI → Tyk/FSG migration** for both Canada and Mexico vendors. Mexico: 4 vendors; some open to change, others lack capacity. Full routing decision (direct to FSG vs through FSG → DAX) still subject to B1/B2 and J3 architecture decisions. | 2026-03-26 (partial) |
| B5 | NAOP vendor inventory feed frequency: Mobile Klinik (Canada) and Mexico vendors push inventory feeds to DAX IVS, but cadence varies across vendors. What is the required frequency to maintain inventory accuracy KPIs? Vendors are expected to account for jobs already booked when sending inventory levels — but without an agreed cadence, accuracy risk grows with lower frequency. | BOM/Availability API Contract Workshop 2026-03-25 | SB Eng / Integration Team | Open | Feed frequency alignment not yet defined. Team to establish minimum cadence requirement during NAOP design sessions (Q4 planning). | |

---

## C. Reservation Architecture

| # | Question | Source | Owner | Status | Answer | Date |
|---|----------|--------|-------|--------|--------|------|
| C1 | **[ARCH]** Who owns the reservation lifecycle end-to-end? ServiceBench initiates soft reservation via IVS; store creates final reservation. Ownership of state transitions must be defined. **Context:** Today SB creates a hard reservation when a job is scheduled. In future state, IVS/DAX would create a soft reservation at availability check time (minutes earlier than job creation). The lifecycle — who creates, who expires, who converts a soft reservation to a hard one, and what happens on rollback — must be clearly owned by one system. Unclear ownership creates risk of double-deduction (SB and DAX both decrement inventory) or orphaned soft reservations that never expire. | Meeting: Reservations Design (Mar 4) | DAX / SB Arch | Open | | |
| C2 | **[ARCH]** Is the 1–5 minute timing gap between availability check and real reservation creation materially impacting business outcomes today? Is soft reservation a near-term necessity or a longer-term optimization? **Context:** The window between when Horizon checks availability (availability check) and when the job is created in SB (reservation confirmed) is roughly 1–5 minutes. During this window, parts could be sold to another customer. If this is a frequent occurrence with business impact (e.g., frequent "out of stock at job creation" errors), soft reservation is critical for Phase 1. If it's rare, it can be deferred. Ops data needed to quantify impact. | Meeting: Reservations Design (Mar 4); **Review Open Questions 2026-03-20** | Bryant / Ops | Open | **Tech Sync 2026-03-20:** SUR today has **no** soft allocation; availability has a **fudge factor**; stores may cancel jobs if parts unavailable; **Raghu** to pull **cancellation metrics** ahead of follow-up session. DES/ISP: **15 min** soft allocation + **double-check** before sales order. | |
| C3 | Should soft reservations apply to all providers or only select ones? **Context:** Some providers can support reservations (UBIF, AOP); others likely cannot (TELUS, Mobile Clinic, NAOP). If soft reservation is provider-specific, the EIAA needs to know which providers support it and return a "not-reservable" flag for the rest. If applied universally, all providers must implement reservation state management — significant scope expansion. | Meeting: Reservations Design (Mar 4) | DAX / SB Arch | Open | | |
| C4 | **[ARCH]** Expiration model for soft reservations: auto-expire after X minutes, or confirmed only by store acknowledgment? What's the rollback if a reservation isn't converted? **Context:** If soft reservations auto-expire, what's the timeout window? Too short and jobs fail; too long and inventory is locked up. If store acknowledgment is required to expire, what happens if the store never acknowledges? Need a clear state machine: soft-reserved → converted (job created) or soft-reserved → expired (timeout or no-show) with inventory restored. | Meeting: Reservations Design (Mar 4) | DAX Arch | Open | | |
| C5 | **[ARCH]** How do we prevent double-deduction during reconciliation when soft reservation is followed by store reservation event? **Context:** If DAX decrements inventory at soft reservation time, and SB also decrements at job creation (store reservation), inventory is deducted twice for the same job. Reconciliation logic must ensure only one deduction occurs. This requires a handshake or state flag between SB and DAX. Without it, inventory accuracy degrades — especially at UBIF scale (~800 locations). | Meeting: Reservations Design (Mar 4) | DAX Arch | Open | | |
| C6 | **[ARCH]** UBIF Legacy: reservation concept is difficult to implement. Option A: return "not-reservable" response for legacy providers. Option B: ServiceBench always reserves, DAX overrides for legacy providers via backend. Which is preferred? **Context:** UBIF Legacy stores (pre-Next Gen Portal) don't have the technical infrastructure to implement the soft reservation handshake. All Legacy stores are expected to migrate to Next Gen by Q3 2026. If this timeline holds, Legacy reservation support may be unnecessary. If any stores slip past Q3, we need a bridge — either returning not-reservable (Option A, cleaner) or SB handling it internally (Option B, no API change). Connects to H6. | Meeting: DAX-SB Integration (Mar 6) | DAX / SB | Open | | |
| C7 | Mobile Clinic: not tracking reservations today. Is it worth adding reservation tracking in the future state, or leave as not-reservable? **Context:** Mobile Clinic uses a different operational model — techs are mobile, not store-based. Reservations require a location-bound inventory pool. If Mobile Clinic doesn't have fixed inventory locations in DAX, reservation tracking may not be meaningful or possible for Phase 1. This decision affects whether Mobile Clinic gets a "not-reservable" flag in the API response. | Meeting: DAX-SB Integration (Mar 6) | DAX / SB | Open | | |

---

## D. JIT Architecture

| # | Question | Source | Owner | Status | Answer | Date |
|---|----------|--------|-------|--------|--------|------|
| D1 | Should the availability API return both in-stock status and JIT eligibility as separate flags per part line, or a single combined response? **Context:** Horizon and SB need to know two things for each part: (1) Is it in stock right now? (2) If not, can it be ordered JIT? These could be one flag ("available or JIT-orderable") or two separate flags ("in_stock: true/false, jit_eligible: true/false"). Separate flags give SB more flexibility for scheduling decisions (e.g., offer JIT appointment with longer lead time). Combined flag is simpler but reduces decision granularity. | Meeting: JIT (Mar 6) | DAX Arch | **Resolved** | **BOM/Availability API Contract Workshop 2026-03-25:** Confirmed as **separate fields** — `qty` (quantity in stock) and `allowJustInTime` (boolean) are distinct response fields per part per location. [ARCH] tag cleared. | 2026-03-25 |
| D2 | Should shipping days out be included in the availability API response (for appointment scheduling by Horizon/ServiceBench)? **Context:** For JIT parts, the lead time (shipping days out) determines how far out an appointment can be scheduled. | Meeting: JIT (Mar 6) | DAX Arch | **Resolved** | **Confirmed in EA Support (2) 2026-03-20 and Part Rel Mgmt 2026-03-19.** DAX availability response will include lead time data. Specific values confirmed: batteries = 7 days, non-batteries = 3 days (configurable by SCM in SB today; will move to DAX product master in future state). F&O already has lead-time concepts at product master level that can be leveraged. | 2026-03-19/20 |
| D3 | Should job type exclusion information be evaluated before the availability check (in the request) or returned in the response for the consumer to act on? | Meeting: JIT (Mar 6); **Tech Sync 2026-03-20** | DAX Arch | **Resolved** | **Post-filter:** DAX returns job type exclusion attributes; **SB applies eligibility logic** (EA Support (2) + Tech Sync 2026-03-20). Formal API contract still finalized in scheduled **Wed** working session with architecture. | 2026-03-20 |
| D4 | Should reservation and JIT order creation be a single combined call or kept as separate API calls? | Meeting: JIT (Mar 6) | DAX Arch | **Resolved** | **2026-03-26: Separate APIs.** Availability API = read-only endpoint. Reservation API = separate writable endpoint (US-2.4). JIT order creation is a separate mechanism. Decoupling keeps APIs composable and avoids coupling read and write paths. [ARCH] tag cleared. | 2026-03-26 |
| D5 | Who maintains the "quantity one" records in ServiceBench for distro and WLI JIT (signals orderability, not true inventory)? Kelly Shaw or Pat Clark? Needs owner before migration. **Context:** In ServiceBench today, JIT ordering is triggered by a special "quantity one" record per SKU — a placeholder that signals the SKU is orderable JIT (not actual inventory). These records exist for both UBIF Distro JIT orders and WLI (third-party AOP) JIT orders. Without an owner, these records won't be maintained accurately after migration, causing JIT orders to fail silently. Part Rel Mgmt 2026-03-19 confirmed JIT applies to both Distro and WLI shipments with the same lead time rules. | Meeting: JIT (Mar 6) | Pat Clark / Kelly Shaw | Open | Part Rel Mgmt 2026-03-19 confirmed JIT scope: applies to both UBIF Distro ordering and 3rd party AOP WLI shipments with same lead time configuration (batteries=7 days, non-batteries=3 days). Owner of "quantity one" records still unconfirmed — specific follow-up with David Woods and Chris O'Donnell scheduled per Part Rel Mgmt action items. | |
| D6 | On-demand ordering for non-AOP providers: is this in scope? Needs confirmation from planning team. **Context:** On-demand ordering = a technician manually orders a single part that isn't in their inventory (not JIT-automated). Today this capability exists for AOP providers. If non-AOP NAOP vendors (e.g., AgioTech, TechPeople) also need on-demand ordering, scope expands to include their ordering workflow in DAX. If excluded, NAOP providers remain on existing ordering mechanisms. | Meeting: JIT (Mar 6) | Planning / Michelle | Open | | |

---

## E. Warehouse & Routing Architecture

| # | Question | Source | Owner | Status | Answer | Date |
|---|----------|--------|-------|--------|--------|------|
| E1 | **[ARCH]** How will vendors identify warehouses in the future-state DAX IVS API? Format standardization needed to match UBIF JSON format. **Context:** UBIF stores identify warehouses using a specific ID/format in the current JSON availability request. DAX IVS uses its own warehouse/location identification scheme. ServiceBench must map between these formats at integration time. Without a defined mapping standard, the availability response won't correctly associate inventory to the store/warehouse location that's being queried, causing incorrect availability results. | Meeting: DAX-SB Integration (Mar 6) | DAX / SB Eng | Open | | |
| E2 | Same UBIF store has same ServiceBench ID for both ISP and SUR. What routing logic will distinguish ISP vs SUR requests? **Follow-on:** UBIF vs **America's** IVS instance routing for warehouse lookup. | Meeting: DAX-SB Integration (Mar 6); **Tech Sync 2026-03-20** | DAX / SB Eng | **Resolved** (SB contract); **engineering follow-up** | **Tech Sync 2026-03-20:** **Line of business** separates ISP vs SUR (repair vs replace): **SUR repair → WLI** in F&O; **replacement ISP → WCF**. Hawaii UBIF example: same store can be SUR + FSL — existing LOB delineation. **ServiceBench** passes store ID; **no SB mapping exercise** required for ISP vs SUR at the integration contract. **Internal DAX follow-up:** PBI for **UBIF IVS vs America's IVS** instance routing when warehouse lookup could hit both (Matt/Michelle — internal DAX/IVS). | 2026-03-20 |

---

## F. SKU / MDM Setup

| # | Question | Source | Owner | Status | Answer | Date |
|---|----------|--------|-------|--------|--------|------|
| F1 | What ServiceBench part setup fields must be replicated in DAX/F&O? (Need complete table: field name, definition, options, mandatory/optional.) Created Mar 16 action item: Amir to send recording; Raghu to present comprehensive field table. **Context:** ServiceBench has a rich Part Master Maintenance UI where each SKU is configured with fields like: shipping days out, job type eligibility (job types not offered), client/retailer linkage (AT&T, Verizon), BOM associations, and equipment type. Every field that drives business logic in SB must have a corresponding field in DAX/F&O, or SB will lose functionality post-migration. The full field table is the foundation for the data mapping work in the TRD. | Meeting: Parts Walkthrough (Mar 16) | Raghu / SCM | Open | Parts Walkthrough confirmed: SB setup is done via file import or Part Master Maintenance UI. Each BOM item linked to specific retailers (AT&T, Verizon, etc.). Fields confirmed as needed: Shipping Days Out, retailer linkage, job type eligibility toggles, part prioritization priority (1-3). Part Rel Mgmt 2026-03-19 confirmed additional fields: job type exclusion attributes, JIT flag, lead time (batteries=7d/non-batteries=3d), client-specific prioritization exceptions. Raghu's comprehensive field table still pending as of 2026-03-20. | |
| F2 | MDM setup process in DAX: is it manual (import or UI) or can it be automated? Who owns MDM setup in the future state? **Context:** Today, part MDM in SB requires either a file import or manual UI entry — no API-driven automation. If DAX requires the same manual process, the "magic tool" (F3) becomes critical to avoid triple-entry (DAX + SB + UBIF Portal). If DAX can accept automated setup via API, the migration and ongoing ops process is significantly simpler. DES/NDS precedent = MDM team owns single setup in DAX. | Meeting: Parts Walkthrough (Mar 16) | MDM Team | Open | Parts Walkthrough confirmed: current SB setup can be done via file import or UI. Future state = MDM owns single setup in DAX (DES/NDS precedent confirmed). MDM team follow-up still required on automation path. | |
| F3 | Is the "magic tool" (single-touch SKU creation across DAX, ServiceBench, UBIF Portal) still a viable concept from the Kaizen event? Who owns it? **Context:** At a prior Kaizen event, a "magic tool" was proposed: a single workflow where MDM sets up a new part SKU once, and it propagates automatically to DAX, ServiceBench, and UBIF Portal. Without this, MDM must perform the same setup 3 times across 3 systems — each with different fields, formats, and access requirements. This is a major operational burden and a significant source of data inconsistency (one system gets updated, others don't). Confirming ownership and viability determines whether MDM automation is a scope item for this project. | Meeting: Parts Walkthrough (Mar 16) | MDM / SCM / Ops | Open | | |

---

## G. Identity & UI for 3rd Party AOP

| # | Question | Source | Owner | Status | Answer | Date |
|---|----------|--------|-------|--------|--------|------|
| G1 | **[ARCH]** DAX MFE in Prism Elite (TypeScript React) vs WMA: which path for rollout to 3rd party AOP providers? | Meeting: JIT (Mar 6); PRD | DAX / Prism | **Resolved** | **DDD-SUR architecture (2026-03-26):** "Migrated to D365 F&O Warehouse Mobile Application; User Access controlled via D365 F&O with Azure Identity management." WMA confirmed. Feature 31.5 (Avengers): "Identity Guesting and F&O user/worker setup for AOP 3rd Party Workers." **Web-based WMA ruled out 2026-03-18.** ISP picking/return to be first-class in UBIF Next Gen Portal (separate track). Whether AOP locations need WMA UI or API-only: H15 (still open). [ARCH] tag cleared. | 2026-03-26 |
| G2 | **[ARCH]** Identity management for 3rd party providers in Prism Elite: UBIF uses @ubifipx.com with Hydra guest accounts into Asurion tenant. What email/identity structure do 3rd party AOP providers use? Should Corey Street be brought in for Hydra guest account design? **Context:** AOP 3rd party providers have high turnover, multiple workers per location, and 9 store locations across the US. The UBIF identity model (Hydra guest accounts via Brent Moore creating user/worker records) may not scale to a separate 3rd party org structure. Alternative approaches: dollar-sign users or non-Asurion tenant users. Before any identity design can start, we need: number of users per location, turnover rate, and who manages 3rd party AOP providers (equivalent to Renee for CA, Alex for MX). Must resolve H16 (Prism access) first. | Meeting: JIT (Mar 6) | Sandeep / Corey Street | **Near resolution** | **DDD-SUR architecture (2026-03-26):** "Requires guesting of ServiceBench user into Azure tenant." Direction = **Azure AD guest accounts** for AOP 3rd party workers in D365 F&O. Feature 31.5 (Avengers) confirms identity guesting + F&O user/worker setup. H15 (UI vs API-only) and H16 (Prism access) still needed before formal resolution. Identity design session with Sandeep/Corey Street required. | 2026-03-26 (partial) |

---

## H. Scope / Product

| # | Question | Source | Owner | Status | Answer | Date |
|---|----------|--------|-------|--------|--------|------|
| H1 | 3rd party price on Job/Claim — what pricing model applies to AOP and NAOP providers in the future state? **Context:** UBIF pricing is resolved (FR-3, US-1.1 Done / US-1.2 Active). 3rd party pricing is now **FR-12** under F-5 (AOP Migration). AOP currently uses zero / consigned pricing. Mobile Klinik: SKU-level price management TBD. Mexico: pricing in consumption messages TBD (possibly BAU). No dev work on US-1.3 until scope confirmed. | PRD §3 | Bryant Mayne | Open | **Updated 2026-03-26:** Scope moved from FR-3 to FR-12 / F-5. UBIF unblocked and in flight. Open items: (a) AOP — zero/consigned or transactional? (b) Mobile Klinik — SKU-level price management; (c) Mexico — BAU or dev needed? US-1.3 Draft; blocked pending this answer. | |
| H2 | BAU Portal feed migration—when/if in scope? **Context:** BAU (Business As Usual) Portal processes NAOP inventory feed data for non-UBIF channels. If this feed migration isn't included in SSOT scope, those channels remain on the legacy SB data pipeline after SSOT goes live — creating a split system where some channels get real-time DAX data and others still get file-based SB data. Timeline conflict: BAU Portal migration may require a separate program of work that needs to be sequenced with SSOT. | Brief §5 | Bryant Mayne | Open | Out of scope until explicitly confirmed. Referenced in PRD §13 Appendix (out of scope list). | |
| H3 | Enterprise architecture assignment: is Ankit / Tim Clemens formally on the project? Leadership has not confirmed EA involvement yet. | Meeting: Parts Walkthrough (Mar 16) | Bryant / Leadership | Resolved | Ankit confirmed engaged: attended SSOT EA Support (1) meeting 2026-03-18, formally reviewing architecture proposal, EA feedback expected 2026-03-19. Tim Clemens named EA co-owner. Bryant scheduling broader architecture review for week of 2026-03-24. EA Support (2) 2026-03-20: Mika (VP EA) confirmed interest; specific allocated resource to be confirmed next week. | 2026-03-18 |
| H4 | Substitution matrix: Tomlin + Robbie Carter + Rose—who owns requirements and configuration in DAX? Meeting to be scheduled upon Tomlin's return. **Context:** In SB today, the part prioritization/substitution matrix is managed by Robbie Carter and Heidi at the SKU level. The matrix ranks parts by priority (e.g., priority 1: OCA, priority 2: pre-build, priority 3: UB glass) to control which inventory depletes first for a given repair type. This matrix is currently client-specific in SB (duplicated for Verizon, AT&T, etc.) but configurations are nearly identical across clients. In DAX, the replacement matrix needs to support: market-level defaults with client-level exceptions. Part Rel Mgmt 2026-03-19 confirmed Matt will create a mock-up to verify the existing replacement matrix can handle multiple parts for the same repair. Blocks EP6-S2. | Meeting: DAX-SB Integration (Mar 6) | SCM / MDM | Open | Part Rel Mgmt 2026-03-19: Discussed using existing replacement matrix with market-level defaults and client-level exceptions (similar to pricing logic). If replacement matrix can't accommodate, may need a separate "part replacement matrix" entity. Matt action item: create mock-up of replacement matrix design to verify. Follow-up meeting with Ankit on part prioritization matrix and client-specific rules scheduled. Blocks EP6-S2 alongside I3. | |
| H5 | Scalability implications for IVS with soft reservations at UBIF scale (~800 locations)? **Context:** UBIF has ~800 store locations, each making availability checks multiple times per day. If soft reservation is layered on top of availability checks, IVS must handle concurrent reservation requests across 800 locations simultaneously. At peak (e.g., morning rush when techs start jobs), this could create significant load on IVS. A performance/scalability analysis is needed before EP4 stories can be groomed. Without it, we can't confirm NFR-2 (performance targets). | Meeting: Reservations Design (Mar 4) | DAX Arch | Open | NFR-2 depends on this. EP4 (soft reservations) cannot finalize design without a scalability analysis. Needed before EP4 stories move from Draft. | |
| H6 | **UBIF Legacy scope:** Do we need to solution for Legacy UBIF at all, given that all stores are expected to fully migrate to Next Gen by Q3? If yes, is the Legacy solution a temporary bridge only, or full parity with Next Gen? ADO Feature 677454 may be out of scope. **Context:** UBIF has two store tiers: Next Gen Portal (modern, actively developed) and Legacy (older flow, being phased out). All stores are expected to migrate to Next Gen by Q3 2026. If every store migrates before SSOT goes live, Legacy support is unnecessary. If stores slip past Q3 (which happens), we need a Legacy bridge — either "not-reservable" flag (C6 Option A) or SB handling internally. Legacy scope also affects EP3-S3 (reservation approach) and RT-2 (UBIF Legacy availability flow). | PRD review (Mar 16) | Bryant / Michelle Bowersox | **Resolved** | **2026-03-25:** UBIF Legacy is **not part of Q2 build scope**. Q2 F-2 is UBIF Next-Gen only. In the event Next-Gen migration is not 100% by end of Q2 (Assumption B), a **Plan B** for addressing Legacy store jobs in DAX is TBD — confirmed last priority for the team. ADO Feature 677454 not being built in Q2. C6 [ARCH] (Legacy reservation) deferred pending Plan B confirmation. Existing Legacy replication feeds remain in place until stores migrate. | 2026-03-25 | |
| H7 | **EP-2 needed?** Is "Part price on Job/Claim" in scope for this project as a standalone epic, or is it already covered under the UBIF orchestration feature (677038)? **Context:** Part prices on job/claim records are needed for store reimbursement (relates to H1). EP-2 stories would migrate UBIF pricing from file-based to transactional pricing via DAX. If EP-2 is a standalone epic, it has its own ADO feature, roadmap slot, and team assignment. If it merges into EP-1 (UBIF orchestration, feature 677038), the stories still exist but the epic boundary and ownership changes. This decision affects backlog organization and HLE. | PRD review (Mar 16) | Michelle Bowersox | **Resolved** | F-1 is a **standalone feature** (Q1, unblocked). Not covered under ADO 677038 (UBIF Next Gen orchestration). Confirmed in roadmap alignment with Michelle/Matt 2026-03-23. | 2026-03-23 | |
| H8 | **EP-4 needed?** Is "Soft reservations via IVS" in scope for this project, or is it a separate enhancement initiative? **Context:** Soft reservations reduce the chance of parts selling out between availability check and job creation (1–5 minute window). If material enough (see C2), it belongs in this project. If the business impact is low, it becomes a Phase 2 enhancement. Confirming scope before Q2 planning prevents grooming effort on stories that may be removed. If confirmed in scope, EP-4 engineering is blocked by **C1, C4, C5** ([ARCH] until resolved). **I2 is resolved** — does not block EP-4. | PRD review (Mar 16) | Michelle Bowersox | Open | **Reconciled:** When EP-4 is in scope, blockers are **C1, C4, C5** only (not I2). If EP-4 is out of scope, remove EP4 stories from TRD. | |
| H9 | AgioTech / TechPeople (NAOP) — confirmed in scope? Same Tyk API contract as other NAOP vendors? **Context:** AgioTech and TechPeople appear in the Parts Mgmt Flows diagram as NAOP (Non-Asurion Owned Parts) vendors. If confirmed in scope, they would use the same INT-3 flow (via Tyk API Gateway) and the same API contract as other NAOP vendors. If out of scope, no integration stories needed. Need Bryant/Amir to confirm whether these vendors are being actively managed and whether their integration is part of the current program. | Parts Mgmt Flows PDF | Bryant / Amir | Open | Appears in Parts Mgmt Flows diagram as a NAOP vendor. If confirmed, they would follow the INT-3 (other NAOP) flow and the same Tyk API contract as INT-1/INT-2. | |
| H10 | EU vendors (AsurionEU / Tesco / VirginMobile) — in scope for this project? Same RTI → Tyk repoint pattern as CA/LATAM? **Context:** EU vendors appear in the Parts Mgmt Flows diagram. If in scope, the integration pattern is the same RTI → Tyk repoint used for CA and LATAM vendors — no separate architecture needed, just scope confirmation and onboarding. If out of scope, EU remains on legacy SB feeds post-launch. Timing: EU scope may depend on regulatory and data residency requirements (EU data must stay in EU). | Parts Mgmt Flows PDF | Bryant / Leadership | Open | Appears in Parts Mgmt Flows diagram. If in scope, pattern is the same RTI → Tyk repoint. No separate architecture needed — confirm scope only. | |
| H12 | US NEW (ATTRepair / Verizon clients) — what is this new client group shown in Parts Mgmt Flows? Scope and timeline for inclusion? **Context:** "US NEW" appears as a distinct client group in the Parts Mgmt Flows diagram alongside existing UBIF clients. It is unclear if this represents: (A) a new repair channel not yet in SB, (B) a rebrand/reorganization of an existing client group (e.g., ATT Tech Express replacement), or (C) a future-state expansion that's still in planning. Without clarification, no integration flow can be scoped, and including them in stories risks building for a group that changes definition. | Parts Mgmt Flows PDF | Bryant | Open | Appears as "US NEW" in Parts Mgmt Flows diagram alongside existing US UBIF clients. Unclear if this is a new channel, a rebrand, or a future-state group. Needs clarification before any INT flow can be scoped for them. | |
| H14 | Order types in scope: Replenishment (auto), Restock (manual), SURJIT (auto) — confirmed in scope alongside On-demand (D6)? Or are only some order types migrating to DAX? **Context:** AOP inventory has four order types today: Replenishment (auto-triggered by inventory level threshold), Restock (manual PO), SURJIT (auto JIT order for SUR parts when stock drops), and On-demand (manual single-unit order). Each has different triggers, workflows, and system dependencies in SB. Confirming which types migrate determines EP-5 story count and complexity. If only a subset migrates, the others stay on SB — creating a split ordering system until fully migrated. | Parts Mgmt Flows PDF | Bryant / Michelle Bowersox | Open | Parts Mgmt Flows PDF and SSOT Parts Mgmt Walkthrough confirmed 4 order types for AOP: Replenishment (auto), Restock (manual), On-demand (manual), SURJIT (auto). On-demand is tracked separately as D6. DDD features 19–24 appear to confirm Replenishment, Restock, and SURJIT. Confirm whether all 4 migrate or only a subset. | |
| H15 | Do 3rd party AOP locations need a WMA inventory management UI, or only systematic API/RTI integration (no UI needed)? **Context:** 9 US AOP stores currently use the ServiceBench mobile app for job updates — not Asurion's Field App or WMA. Web-based WMA was evaluated and ruled out 2026-03-18 (certification burden, performance, user management). Native WMA app is confirmed for AOP back-of-house inventory. But the question remains: do AOP 3rd parties need any user-facing UI for inventory management, or do they operate entirely through systematic API/RTI data exchange? If API-only, EP5-S2 scope changes significantly and WMA rollout planning for AOP changes. Thays action item: gather work instructions from 3rd party providers on current inventory processes. **Legal constraint added (HLE 2026-03-25):** ISP providers currently cannot access DAX UI due to data visibility concerns (seeing too much Asurion data). Same legal/data risk applies to AOP 3rd party providers — if WMA is the path, a gap analysis is required to confirm the WMA tool exposes only the fields AOP providers need, without exposing excess Asurion data. | WMA ISP / 3rd Party meeting 2026-03-18; HLE 2026-03-25 | Bryant / Avengers | Open | Per meeting: 9 US AOP stores currently use ServiceBench mobile app for job updates (not Field App). Web-based WMA ruled out. **HLE 2026-03-25:** Legal precedent — ISP cannot access DAX UI. Same risk applies to AOP. WMA gap analysis required before UI path is confirmed. If AOP locations only need API-level inventory sync with no user-facing UI, EP5-S2 scope changes significantly. | |
| H16 | Will 3rd party AOP locations have Prism UI access? **Context:** Before designing the identity model for AOP 3rd party providers (G2), we need to know if they'll use Prism UI at all. If AOP locations don't get Prism access, the Hydra guest account design (G2) is moot — only API/RTI access is needed. If they do get Prism access, the identity model must support high-turnover, multi-user-per-location scenarios across 9 stores. This must be answered before Sandeep/Suman can begin identity architecture design (EP5-S3). | WMA ISP / 3rd Party meeting 2026-03-18 | Bryant / SB Team | Open | Action item from 2026-03-18 WMA meeting: clarify with ServiceBench/Prism team. Impacts G2 (identity/access model) and EP5-S3 (Hydra identity story). Must be answered before identity design can proceed. | |
| H17 | **⚠️ AOP company count: 8 or 9?** 3rd Party Service Providers session (2026-03-24) confirmed 9 companies; HLE discussion (2026-03-25) referenced 8 companies. PRD and Scope §5 currently state 9. Which number is correct? Does 8 exclude one company that is no longer active, or is 9 the correct figure including a company in onboarding? | 3rd Party AOP 2026-03-24; HLE 2026-03-25 | Bryant / Geraldine Drennon | Open | Discrepancy between 03/24 and 03/25 sessions. Needs clarification before warehouse setup scoping. | |

---

## I. API Architecture

| # | Question | Source | Owner | Status | Answer | Date |
|---|----------|--------|-------|--------|--------|------|
| I1 | **[ARCH]** BOM Lookup architecture: embed BOM lookup inside the EIAA (Early Inventory Availability API) as a single orchestrated call (DDD Option 2 / feature 3) — OR — expose as a separate BOM service that ServiceBench calls first (DDD Option 1 / feature 1)? | DDD Feature 32; EA Support 2026-03-18/20; **Tech Sync 2026-03-20** | Ankit / DAPI Team / Integration Team | Open | **Tech Sync 2026-03-20:** **DAX owns** returning the list of parts from device/BOM (**BOM API vs combined "God API"** = two technical options; either way **data is on DAX side**). SB/Prism **consume** response; **decisions on use of data are SB/Prism**. Raghu: pass-through from Cloud API (device + peril) → SB maps to part types → DAX. Matt: API currently expects **part types**, not perils — **Wed** session for tag-level contract. **Michelle** producing **line-by-line API contract** (MA project style) for Wed technical call. **Formal contract** = still pending that session — keep **[ARCH]** until signed off. | |
| I2 | AOP JIT reservation: is a reservation required for AOP JIT scenarios when inventory availability is false? DDD open item 33 leans toward no reservation in that case. Decision needed before EP-4/EP-5 stories can be groomed. **Context:** When an AOP tech orders a JIT part (inventory = 0, but JIT-orderable = true), should a reservation be placed in DAX? A reservation on a zero-quantity item has no inventory to hold — it's a placeholder. DDD open item 33 direction: no reservation needed when availability is false for JIT parts. If confirmed, EP-4 and EP-5 blockers on I2 are cleared, simplifying the JIT ordering flow significantly. | DDD Feature 33 | Avengers / DAX Arch | Resolved | **Part Rel Mgmt 2026-03-19 confirmed: No reservation needed for out-of-stock JIT-enabled parts.** This aligns with DDD open item 33 direction. EP4-S1 and EP5-S1 blockers on I2 cleared. Decision from Avengers team + DAX Arch. | 2026-03-19 |
| I3 | Part Substitution Matrix vs Replacement Matrix: reuse existing Replacement Matrix with modified unique index/validation rules — OR — build a new F&O menu/form/table/data entity for part substitution? **Context:** In SB today, the substitution matrix ranks alternative parts for a repair (e.g., if priority 1 OCA is out, use priority 2 pre-build). In DAX/F&O, the "Replacement Matrix" is an existing entity that maps one item to another for product lifecycle scenarios. The question: can the Replacement Matrix be adapted to support part substitution with priority rankings (1-3 per part per repair type), market-level defaults, and client-level exceptions? Or is it so structurally different that a new entity is needed? DDD open item 34 leans toward reuse with modification. Part Rel Mgmt 2026-03-19: explored reuse approach with market-level defaults + client-specific exceptions (like pricing logic). Matt action item: create mock-up to validate. **Additional complexity (BOM/API workshop 2026-03-25):** A separate part-substitution API with **reverse lookup** capability may be needed — i.e., given a substitute SKU (e.g., SKU3), find what original part it substitutes for. This is required to handle the edge case where a job is created with a priority-2 part (due to availability) but the BOM only returns priority-1. VFF requirement: keep existing SKU on job if repair eligibility is for the same SKU. **Separate session scheduled** to detail this API contract. | DDD Feature 34; BOM/API workshop 2026-03-25 | Avengers / Robbie Carter / Rose | Open | DDD open item 34: leading option is reuse of Replacement Matrix with modified validation. Part Rel Mgmt 2026-03-19: market-level defaults + client-level exceptions approach being explored. Matt action item: create mock-up. **BOM/API 2026-03-25 adds:** reverse lookup requirement (given substitute SKU → find original) and edge-case handling for priority-2 parts on active jobs. Follow-up session to be scheduled. Blocks EP6-S2 alongside H4. | |
| I4 | **Prism peril-change scenario:** When UBIF calls Prism to add or change a peril on an existing job, which path should Prism take? **Option 1A:** Prism calls the same combined BOM+availability API and ignores qty on-hand information (simplest — single API, no BOM enhancement needed). **Option 2B (renamed):** Prism calls the existing BOM lookup to get parts and product equipment type (keeps BOM and availability calls separate for peril-change use case). **Context:** The main combined API design was confirmed 2026-03-25 (DDD Option 2 / meeting's Option 1). But the peril-change sub-scenario — where UBIF triggers Prism to add a different peril to an in-progress job — requires a separate decision on whether to reuse the combined call or enhance the BOM lookup. | BOM/Availability API Contract Workshop 2026-03-25 | BeyondX / DAPI / Michelle Bowersox | Open | Discussed but not resolved at 2026-03-25 session. Separate follow-up to be scheduled. Does not block Q2 main API build but must be resolved before ServiceBench peril-change flow is implemented. | |

---

## J. Product Intelligence & Remote Tech (New — from EA Support (2) and Part Rel Mgmt, 2026-03-19/20)

| # | Question | Source | Owner | Status | Answer | Date |
|---|----------|--------|-------|--------|--------|------|
| J1 | **[ARCH]** Product 360 / Product Intelligence team (Julian Bankston's org) is building a device catalog initially for Amazon and expanding to phones and other device categories. Does this overlap with DAX part master and how Asurion SKU inventory is managed in this project? **Context:** At enrollment time, Product 360 envisions calling their catalog API to get device attributes, feeding into Assurance Queue and Horizon. This could affect how device-to-part mapping is sourced — currently this lives in DAX (device + LCM parts + BOM). Mark Zoukas and Raj are doing related Product Information Management work for connected home suppliers. Risk: multiple teams building overlapping device/part catalog solutions. Team consensus (EA Support (2)): won't stall SSOT — DAX will still manage Asurion SKU inventory regardless of upstream solutions. But boundary must be defined. Next step: Bryant to schedule discussion with Mika (VP EA), Mark Zoukas, Julian's team to set scope boundaries. | SSOT EA Support (2) 2026-03-20 | Bryant / Ankit / Mika | Open | Team agreed 2026-03-20: SSOT analysis continues unblocked. DAX inventory management for Asurion SKUs is distinct from upstream device catalog. Bryant to schedule boundary-setting discussion. Won't stall current work but must be resolved before architecture is finalized. | 2026-03-20 |
| J2 | Remote Tech 3rd party identity management: 9 companies, ~100 locations, ~500+ remote techs performing ~6,000 jobs/month (all OSR). How do these providers authenticate and manage users in the future state? **Context:** Unlike AOP 3rd party providers (G2/H16 — 9 stores doing inventory management), Remote Tech 3rd parties are field technicians performing on-site repairs across ~100 locations. They currently use ServiceBench for job management. Their identity needs are different from AOP (no WMA or Prism needed — they're job-flow users). Part Rel Mgmt 2026-03-19 provided landscape: ~500+ techs across 9 companies. Action item: follow-up with Sandeep and Suman on identity management approach for 3rd party remote tech users. Separate from G2 (which covers AOP inventory management). | Part Rel Mgmt 2026-03-19 | Sandeep / Suman | Open | Part Rel Mgmt 2026-03-19: landscape confirmed — 9 companies, ~100 locations, ~500+ techs, ~6,000 OSR jobs/month. Follow-up with Sandeep/Suman scheduled as action item. Separate workstream from AOP identity (G2/H16). | 2026-03-19 |
| J3 | Mexico/RTI routing path: Can Mexico vendors move directly to FSG, or must RTI webservice route through FSG to DAX? **Context:** Mexico vendors currently use RTI webservice API calls for inventory integration (unlike US/Canada vendors who use file-based feeds). When their integration migrates to DAX, the RTI webservice calls need to be rerouted. Two paths: (A) Mexico vendors migrate directly to FSG (clean break, requires Mexico-side integration work); (B) RTI routes through FSG to DAX (intercepts existing RTI calls, less Mexico-side change). Path B is less disruptive but adds an RTI-FSG proxy layer. Path A requires vendor cooperation. Part Rel Mgmt 2026-03-19 action item assigned. See also B4. | Part Rel Mgmt 2026-03-19 | SB Eng / Mexico rel. | Open | Part Rel Mgmt 2026-03-19 action item: SB team to determine if Mexico vendors can move to FSG directly or if RTI needs to route through FSG to DAX. No decision yet. | 2026-03-19 |

---

## K. New questions — DDD architecture ingestion (2026-03-26)

| # | Question | Source | Owner | Status | Answer | Date |
|---|----------|--------|-------|--------|--------|------|
| K1 | Depot / Mail-in Repair (~1% of US SUR jobs per SCM channel mix, avg ATT + Verizon FY 2026): is this channel in scope for SSOT-SUR? If so, which phase/epic covers it? Is it part of NAOP (Q4 F-6), a separate US depot track, or out of scope? Note: this is a US channel — distinct from NAOP Canada/Mexico vendor volume which is TBD. | SCM channel mix (David Tomlin 2026-03-25) | Bryant / Michelle Bowersox | Open | | |
| K2 | NAOP job types — Mobile Klinik (Canada) and Mexico serve Depot/Carry-In only (no Remote Tech) per Parts Management Flows. Does this limit what inventory data is required from NAOP vendors? Does it affect job type exclusion logic for NAOP in FR-4 and RT-3? | Parts Management Flows PDF (Flow 2A: Mobile Klinik — "Depot, Carry-In; no Remote Tech"; Flow 2B: Mexico — same) | Bryant / Amir / Raghu | Open | Implication: NAOP inventory check only needed for Depot and Carry-In job types. Remote Tech jobs for Canada/Mexico clients use AOP/UBIF channels, not NAOP. | 2026-03-26 |
| K3 | UBIF IVS instance for F&O is not currently enabled — DDD Feature 13: "Stand up and enable the UBIF IVS instance for F&O data source (currently not enabled)." What is the timeline and team ownership for enabling UBIF IVS? This is a Q2 technical prerequisite. | DDD Feature Breakdown F.13 (Configuration) | DAPI / Integration Team / Ankit | Open | Blocks core orchestration layer (Feature F.6) and UBIF Next-Gen availability (RT-1). | 2026-03-26 |
| K4 | Americas IVS vs UBIF IVS routing/mapping rules — DDD Feature F.9: DAX API must determine which backend (UBIF IVS vs Americas IVS) for each ServiceBench location. Americas WLI has UBIF stores as external warehouses — must be excluded from Americas IVS lookups (zero inventory there). Feature F.23: technical exploration needed for consolidating mapping rules (Americas company/site/warehouse + UBIF store site/warehouse/company + UBIF Part SKU mapping from Asurion SKU). | DDD Feature Breakdown F.9 and F.23 | DAPI / ETL Team | Open | E2 partially covers LOB routing; this is the deeper warehouse/mapping layer. Blocks F.6 (core orchestration). | 2026-03-26 |

---

## L. Metrics, Reporting & Operational Risk (New — from SUR SSOT Strategy Discussion 2026-03-26)

| # | Question | Source | Owner | Status | Answer | Date |
|---|----------|--------|-------|--------|--------|------|
| L1 | **Repair funnel reporting broken:** The repair funnel reporting tool has been broken for several weeks following Prism migration changes; the role responsible for maintaining it (Ross) was eliminated. Who owns fixing it, and what is the risk to SSOT success metric tracking? **Context:** SSOT benefit is measured in part by conversion rate improvements and shrink reduction. Without a functional repair funnel report, baseline and post-launch measurement is unreliable. Before SSOT launches, a working funnel report (or a replacement baseline metric) is required. | SUR SSOT Strategy Discussion 2026-03-26 | Bryant / Rebecca Suttler / Mike Carter | Open | Repair funnel broken since Prism migration (new status events + system incompatibilities). Customer experience ownership shifted to Rebecca Suttler and Mike Carter under new enterprise structure. No active maintenance underway. Risk: SSOT benefit cannot be measured without a baseline. | |
| L2 | **SUR job volume decline:** Asurion SUR job volume has declined — likely inventory-related. Is this decline caused by the Prism migration (new status events, reconciliation issues), inventory data quality issues, or a separate business driver? **Context:** If volume decline is Prism-related, SSOT's DAX inventory fix may not address it fully. If it is inventory-related, SSOT directly addresses the root cause. Establishing the root cause before SSOT launches ensures the project's success metrics are correctly scoped and not credited for (or blamed for) unrelated volume changes. | SUR SSOT Strategy Discussion 2026-03-26 | Bryant / Client team | Open | UBIF client team currently reviewing only lagging P&L data (after month close) — no leading indicators monitored. Recommended: investigate systematic Prism changes and establish top-of-funnel conversion rate tracking before SSOT baseline is set. | |

---

**How to use with Cursor**

- Say "capture the answer: [question ID] was resolved: [answer]" and the agent will update the row and suggest edits to the PRD/Brief.
- Questions tagged `[ARCH]` block the relevant FR from being finalized. Resolve architecture decisions before grooming stories.
- **Recent meetings:** **SUR SSOT Strategy Discussion 2026-03-26** (distro migration added as PRD §6 dependency; **L1** added — repair funnel broken; **L2** added — SUR volume decline risk; 3rd party AOP shrink = ~$400k/yr, root cause = field mismanagement). **Deep dive BOM/Availability API contracts 2026-03-25** (full API contract confirmed; **I4** added — Prism peril-change 1A vs 1B sub-question; **B5** added — NAOP feed frequency; 3rd party reservation flow internal discussion flagged). **HLE Discussion 2026-03-25** (dev sizing: API=2sp, Reservation=1-2sp, SubMatrix=2sp, JIT=1sp; flip-fold BAT edge case; multi-part reservation; cancellation latency; composite + granular API decision; **D4 resolved** — separate APIs; legal constraint for AOP DAX access; **H17** added — company count discrepancy). **BOM/Availability API Contract 2026-03-25** (combined API confirmed; PRISM location IDs = existing SB numeric values; **D1 resolved**; reverse lookup for substitution matrix identified — **I3 updated**). **Replenishments/MIN-MAX 2026-03-24** (min-max, JIT, scheduling confirmed for D365). **3rd Party AOP 2026-03-24** (scope: 9 companies; user provisioning; legal review flagged). Cross-ref: PRD §12 and TRD §10/§12 match `open-questions.md` after each reconcile pass.
